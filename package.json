{
  "private": true,
  "workspaces": [
    "frontend-react",
    "server-refactored",
    "single_test"
  ],
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "dev": "concurrently \"cd frontend-react && yarn dev\" \"cd server-refactored && yarn dev\" \"cd single_test && npm start  \"",
    "devBackend": "cd server-refactored && yarn dev",
    "devFrontend": "cd frontend-react && yarn dev"
  }
}
